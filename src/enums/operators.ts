export enum OperatorsTypeNumEnum {
  EQUAL = "=",
  NOT_EQUAL = "!=",
  GREATER_THAN = ">",
  LESS_THAN = "<",
  GREATER_THAN_OR_EQUAL = ">=",
  LESS_THAN_OR_EQUAL = "<=",
}

export enum TextOperatorsEnum {
  CONTAINS = "contém",
  NOT_CONTAINS = "não contém",
  EQUAL = "=",
  NOT_EQUAL = "!=",
}

export enum DateOperatorsEnum {
  EQUAL = "=",
  NOT_EQUAL = "!=",
  BETWEEN = "entre",
  GREATER_THAN = ">",
  LESS_THAN = "<",
}

export enum BoolOperatorsEnum {
  EQUAL = "=",
}
